### Creare servizio database de mongodb
`sudo apt install docker-compose`
`docker-compose -f progetti/mongodb_lesson/mockups/docker-compose.yml up`

### Per farlo andare
`docker start mongo`
`docker start mongo-express`